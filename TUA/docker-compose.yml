version: "3.8"

services:
  mysql:
    image: mysql:8.2
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_HOST=mysql
      - MYSQL_DATABASE=siatka
      - MYSQL_USER=user
      - MYSQL_PASSWORD=password
    volumes:
      - ./init-db-mzl.sql:/data/application/init.sql
    ports:
      - "3306:3306"
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-ppassword"]
      interval: 5s
      timeout: 30s
      retries: 3
    command: --init-file /data/application/init.sql

  wildfly:
    image: jboss/wildfly:25.0.0.Final
    build:
      context: .
      dockerfile: DockerfileXD
      args:
        - WILDFLY_USER=dupa
        - WILDFLY_PASS=jasia
        - DB_NAME=siatka
        - DB_USER=siatka
        - DB_PASS=siatka
        - DB_URI=mysql:3306
        - MYSQL_VERSION=8.2.0
    ports:
      - "8080:8080"
      - "8443:8443"
      - "9990:9990"
#    volumes:
#      - ./target/ssbd-group4-project.war:/opt/jboss/wildfly/standalone/deployments/ssbd-group4-project.war:ro
    env_file:
      - ./.env
    environment:
      - WILDFLY_ADMIN_USER=admin
      - WILDFLY_ADMIN_PASSWORD=secret
      - WILDFLY_USER=admin
      - WILDFLY_PASS=adminPassword
      - DB_NAME=siatka
      - DB_USER=user
      - DB_PASS=password
      - DB_URI=mysql:3306
    networks:
      - app-network
    depends_on:
       mysql:
         condition: service_healthy

networks:
  app-network:
    driver: bridge